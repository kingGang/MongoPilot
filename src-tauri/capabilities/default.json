{
  "identifier": "default",
  "description": "Default capabilities for MongoPilot",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:default",
    "dialog:allow-save",
    "dialog:allow-open"
  ]
}
